global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "iso-engine"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["iso-engine:8080"]
        labels:
          application: "iso8583-payment-switch"
          environment: "production"
